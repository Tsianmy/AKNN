## Base

Base module for approximate nearest neighbor search with AVX intrinsics and candidate pool implemented by insertion sort.

##### Usage

```bash
mkdir build
cmake -B build/
make -C build/
./AKNN.exe
```

